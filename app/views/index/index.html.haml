%h1 TheBlockBot

%p !!! Alpha version software, your input may be wiped !!!

- if current_user
  = button_to 'Subscribe to block lists',
    block_lists_path,
    :class => 'btn btn-primary'
- else
  =  button_to 'Sign In with Twitter',
    '/auth/twitter',
    :class => 'btn btn-primary'
